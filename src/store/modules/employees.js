const state = {
  title: "Employee List",
};

const getters = {
  // data store in state
  employeesTitle: (state) => {
    return state.title;
  },
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
